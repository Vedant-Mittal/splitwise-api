services:
  - type: web
    name: splitwise-api
    env: node
    buildCommand: npm install
    startCommand: node simple-server.js
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://vedant-mittal.github.io,http://localhost:3000 